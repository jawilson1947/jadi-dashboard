import { Placeholder } from "@/components/layout/Placeholder";

export const metadata = { title: "Student Lookup" };

export default function Page() {
  return <Placeholder title="Student Lookup" phase={5} spec="§10" />;
}
