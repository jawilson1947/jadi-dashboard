import { Placeholder } from "@/components/layout/Placeholder";

export const metadata = { title: "DNR / DNC Analysis" };

export default function Page() {
  return <Placeholder title="DNR / DNC Analysis" phase={3} spec="§8" />;
}
